Handoff 3 (`docs/design_handoff_fixes_and_schools/`, ids 5a/6a/8a) engine surface. Three changed blocks, no new blocks — no fence seeding required this release.

- **Changed `styles-card-v2`** (§5a layer color system): the card's soft shadow now carries the layer's color (a second `0 3px 14px -4px` shadow layer in `color-mix(… 45%, transparent)`), and the header id pill is tinted with the layer color (`color: var(--layer-accent, …)` on a `currentColor` 9% wash) instead of flat blue/grey. New `.card-nearest-tag` (600-weight colored lead) and `.card-nearest-note .card-sep` rules for the §8a row vocabulary.
- **Changed `card-helpers`**: new display formatters `cardTitleCase(str)` (ALL-CAPS feed normalizer — single letters, digit-carrying tokens, and a keep-caps set `ES/MS/HS/PS/IS/NE/NW/SE/SW/IL/NY/CA/US` pass through; never expands abbreviations) and `cardGradeRange(str)` (contiguous comma grade list → "PK–8"; gapped/unrecognized lists render unchanged). `renderNearestRows` items accept optional `tag: {text, color}` (colored lead on the note line) and `accentColor` (tints the distance pill: color + 10% background wash).
- **Changed `school-zone-factory`** (§6a): new `opts.titleCaseData` — title-cases name/address for ALL-CAPS feeds (inert unless passed). The redesigned (data-only-opts) card path now declares `cardIdentifier`: the collapsed grade range renders as the header pill and no longer trails the address in the body note. Legacy caller-HTML path untouched.
- Consume-side scripts are unchanged.
- Fork adoption: none required — the shadow/pill tint restyles at bump, the new helpers/opts are inert until registrations use them. **Observable change without any fork edit**: a school-zone layer on the redesigned card path whose feed carries grades gains a grade-range header pill at bump (that is §5b/§6a working as intended, not drift). Recommended follow-ups in each fork's card pass: set `titleCaseData: true` on ALL-CAPS zone feeds, and rebuild bespoke school-location cards on the §8a/8b chips pattern (Chicago's is the reference: chips + `renderNearestRows` tag/dot/accent slots + display-only header suffix).
